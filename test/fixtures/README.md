# Test Fixtures

Sample projects and golden files used to verify the Detector, Generators, and
Executor. See the [Test Strategy](../../docs/testing/test-strategy.md) for
conventions.

Naming: `<language>-<package-manager>-<variation>/` (e.g. `node-pnpm-basic/`).

_No fixtures yet — they are added with the Detector Engine specification._
