# demo-no-tests

Minimal service without tests for Pipeline Doctor. No external dependencies; Node.js 20+ required.

```bash
npm ci
npm run lint
npm run build
npm start
```

The server serves JSON on port 3000; override with `PORT`. Stop with Ctrl+C.
