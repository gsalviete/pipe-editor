{
  "name": "node-npm-nest-basic",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.0.0",
  "engines": { "node": "20" },
  "scripts": {
    "build": "tsc",
    "lint": "echo lint && exit 0",
    "test": "echo test && exit 0"
  }
}
