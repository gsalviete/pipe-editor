{
  "name": "demo-api",
  "version": "1.0.0",
  "private": true,
  "description": "Passing API pipeline with GitHub Actions import",
  "engines": {
    "node": "20"
  },
  "packageManager": "npm@10.0.0",
  "scripts": {
    "lint": "node --check src/main.js",
    "build": "node build.js",
    "start": "node dist/main.js",
    "test": "node --test"
  }
}
