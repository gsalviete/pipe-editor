# demo-api

Passing API pipeline with GitHub Actions import. No external dependencies; Node.js 20+ required.

```bash
npm ci
npm run lint
npm test
npm run build
npm start
```

The server serves JSON on port 3000; override with `PORT`. Stop with Ctrl+C.
