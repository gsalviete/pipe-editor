---
id: TASK-043
title: "[DOC] Rewrite REPORT.md from measured gates (AR-31)"
status: todo
parent: docs/product/06-hardening-v2.md#in
labels: [docs, phase-6]
priority: 3
blocked_by: [TASK-040, TASK-041, TASK-042, TASK-035, TASK-036, TASK-037, TASK-038, TASK-039]
created: 2026-09-23
---

> *This was generated by AI.*

# [DOC] Rewrite REPORT.md from measured gates (AR-31)

## What this card solves

`REPORT.md` claims the project is "hardened", "green on every gate", has "one open architectural item", and that "nothing above blocks the product". The review showed those claims were too strong: at the time, the frontend image didn't even build.

This last card of Hardening v2 rewrites the report from **freshly measured** results. It runs every gate from a clean checkout on the CI toolchain (Node 20) and states what is proven and what isn't. The adversarial review and `docs/hardening-report.md` stay unchanged as history, per the product doc.

## Test criteria

Docs card with measured inputs. It's done when:

```
describe('REPORT.md reflects reality', () => {
  it('every number in §3 comes from a run recorded in this card\'s handoff (command + date + result)')
    // pnpm check; docker build -f backend/Dockerfile .; docker build -f frontend/Dockerfile .; compose smoke; test counts
  it('each AR-01…AR-32 is listed with its closing test IDs and gate')
  it('no unqualified "hardened" / "green on every gate" / "nothing blocks" claim remains')
  it('limits and §7 follow-ups of the review are listed as knowingly unfinished')
  it('adversarial-review.md and hardening-report.md are unchanged (git diff empty for them)')
})
```

## Implementation plan

- Run on Node 20 (CI version), from a clean clone:
  - `pnpm install:all`
  - `pnpm check`
  - the `images:` gate
  - the Compose smoke test from `.github/workflows/ci.yml`
  - the audits from TASK-041
- Rewrite `REPORT.md`, keeping its section structure:
  - §1 executive summary (`:18`);
  - §3 verified state (`:106`);
  - §5 security posture (`:229`);
  - §8 limits (`:315`);
  - §9 unfinished (`:337`) lists the review §7 follow-ups and the product doc's Out list.
- Update the spec statuses and the traceability table in `docs/testing/test-strategy.md` for every criterion the phases covered (CLAUDE.md workflow step 5), if earlier cards left any row behind.
- `REPORT.md` is currently untracked in git. The owner decides whether to commit it.
- Out of scope: new fixes. If a gate fails here, stop and open a `[FIX]` card; don't paper over it in the report.

## Attention points

- Honest claims (ADR-0001): local validation runs the same steps in containers. Keep the report free of any hosted-runner parity claim, and state that hosted GitHub and GitLab runs weren't dispatched unless the owner did so manually.
- ❌ Don't quote historical numbers as current.
- The report's header table claims "Source of truth: commit fae6a98b (clean)". Update it to the measured revision.

## Observability

No observability requirements — documentation only.
