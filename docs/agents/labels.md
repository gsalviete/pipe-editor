# Labels

```
n/a (markdown mode — frontmatter labels are free-form)
```

No remote tracker is in use, so there is no platform for labels to live on. CI
(`.github/workflows/ci.yml`) enforces no labels.
